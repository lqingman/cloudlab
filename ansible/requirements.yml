---
collections:
  # Pinned to versions compatible with ansible-core 2.17 (control node Python 3.10).
  - name: community.general   # ufw module
    version: ">=8.0.0,<11.0.0"
  - name: ansible.posix       # authorized_key module
    version: ">=1.5.0,<3.0.0"
