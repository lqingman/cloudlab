# Secrets / credentials — never commit
*.pem
*.key
id_ed25519*
id_rsa*
.env
*.tfvars
terraform.tfstate*
.terraform/

# Personal config / share links
*-client.json
secrets/

# Ansible — real (secret) vars and local inventory; only *.example is committed
ansible/group_vars/all.yml
ansible/inventory/hosts.ini
*.retry
